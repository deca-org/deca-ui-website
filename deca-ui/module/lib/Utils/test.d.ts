export declare const remToPx: (remVal: string | number) => string;
export declare class Test {
    static color(inputColor: string): string | number;
    static size(inputSize: string): string;
    static space(inputSpace: string): string;
    static fontSize(inputFontSize: string): string;
    static borderRadius(inputBorderRadius: string): string | number;
    static font(inputFont: string): string | number;
    static breakpoint(inputBreakpoint: string): number;
    static lineHeight(inputLineHeight: string): string;
    static fontWeight(inputFontWeight: string): string | number;
}
